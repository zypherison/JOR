{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities for all JOR windows",
  "windows": ["main", "clipboard", "settings", "tos"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}
